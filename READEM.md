# GitHub Branch Commands 

git branch (checkout the branch)<br>
git branch -M main (rename branch)<br>
git checkout <-branch name -><br>
git checkout -b <- new branch name -> (Create new branch directory)<br>
git checkout -d <- branch name -> (Delete the branch)