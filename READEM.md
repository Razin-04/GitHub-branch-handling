# GitHub Branch Commands 

git branch (checkout the branch)
git branch -M main (rename branch)
git checkout <-branch name ->
git checkout -b <- new branch name -> (Create new branch directory)
git checkout -d <- branch name -> (Delete the branch)